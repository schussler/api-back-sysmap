package com.sysmap.hubapi.service;

import org.springframework.stereotype.Service;

@Service
public class ParticipantService {
    // TODO: subscribe(UUID activityId) → SubscriptionResponse
    // TODO: approve(UUID activityId, ApproveRequest) → void
    // TODO: checkIn(UUID activityId, CheckInRequest) → void
    // TODO: unsubscribe(UUID activityId) → void
}
