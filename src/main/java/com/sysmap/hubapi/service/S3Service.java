package com.sysmap.hubapi.service;

import org.springframework.stereotype.Service;

@Service
public class S3Service {
    // TODO: uploadFile(MultipartFile file, String folder) → String (URL pública)
    // TODO: validateImageFormat(MultipartFile file) → void (lança InvalidImageFormatException se não PNG/JPG)
}
