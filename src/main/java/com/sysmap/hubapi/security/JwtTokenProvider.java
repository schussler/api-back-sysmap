package com.sysmap.hubapi.security;

import org.springframework.stereotype.Component;

@Component
public class JwtTokenProvider {
    // TODO: generateToken(User user) → String
    // TODO: getUserIdFromToken(String token) → UUID
    // TODO: validateToken(String token) → boolean
}
